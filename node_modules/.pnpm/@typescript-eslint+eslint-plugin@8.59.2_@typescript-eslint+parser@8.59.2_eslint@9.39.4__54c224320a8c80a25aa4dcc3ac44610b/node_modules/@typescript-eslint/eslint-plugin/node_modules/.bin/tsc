#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../typescript/bin/tsc" "$@"
else
  exec node  "$basedir/../../../../typescript/bin/tsc" "$@"
fi
# cmd-shim-target=/home/denisson-pereira/Documentos/kozmo-cardapio-front/node_modules/.pnpm/@typescript-eslint+eslint-plugin@8.59.2_@typescript-eslint+parser@8.59.2_eslint@9.39.4__54c224320a8c80a25aa4dcc3ac44610b/node_modules/typescript/bin/tsc
